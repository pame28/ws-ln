import { Component, OnInit, Output, EventEmitter } from '@angular/core';
import { Router, NavigationEnd } from '@angular/router';
import { filter } from 'rxjs/operators';

@Component({
  selector: 'app-side-bar',
  templateUrl: './side-bar.component.html',
  styleUrls: ['./side-bar.component.css']
})
export class SideBarComponent implements OnInit {
  @Output() sidebarToggled = new EventEmitter<boolean>();
  
  isCollapsed = false;
  currentRoute = '';
  
  // Menú básico - puedes modificarlo según tus rutas reales
  menuItems = [
    { label: 'Inicio', icon: 'fa-home', link: '/home', exact: true },
    { label: 'Dashboard', icon: 'fa-chart-line', link: '/dashboard' },
    { label: 'Usuarios', icon: 'fa-users', link: '/users' },
    { label: 'Configuración', icon: 'fa-cog', link: '/settings' },
    { label: 'Ayuda', icon: 'fa-question-circle', link: '/help' }
  ];

  constructor(private router: Router) {}

  ngOnInit() {
    // Inicializar estado del sidebar
    const savedState = localStorage.getItem('sidebar-collapsed');
    if (savedState) {
      this.isCollapsed = savedState === 'true';
    }
    
    // Escuchar cambios de ruta para marcar el item activo
    this.router.events
      .pipe(filter(event => event instanceof NavigationEnd))
      .subscribe((event: any) => {
        this.currentRoute = event.url;
      });
  }

  toggleSidebar() {
    this.isCollapsed = !this.isCollapsed;
    this.sidebarToggled.emit(this.isCollapsed);
    localStorage.setItem('sidebar-collapsed', this.isCollapsed.toString());
  }

  navigateTo(link: string) {
    this.router.navigate([link]);
  }

  // Verificar si un item del menú está activo
  isActive(link: string, exact: boolean = false): boolean {
    if (exact) {
      return this.currentRoute === link;
    }
    return this.currentRoute.startsWith(link);
  }
}